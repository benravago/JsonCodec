
## JSON Encoder/Decoder ##

Simple JSON support functions similar to python's `json.loads()`, `json.dumps()`, etc.

The JSON Decoder tries to convert JSON text into basic java objects like Map, List, String, Number, and Boolean and the Encoder does the reverse.
